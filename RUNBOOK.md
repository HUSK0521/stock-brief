# Phase 1 操作手冊

目標:**14 天內讓 5 個陌生人付錢。** 沒過就不要進 Phase 2。

樣本頁(已發布,會收匿名問卷):
https://claude.ai/code/artifact/4ae34ac1-e44c-4c56-91fc-50e8005156d0

---

## 先做一件事(2 分鐘)

去 https://finmindtrade.com 免費註冊拿 token,然後:

```bash
export FINMIND_TOKEN=你的token
```

不設也能跑,但免費層流量上限很容易撞到(我測試時撞了)。**撞到時程式會拒絕輸出並回傳非 0**,不會給你一份殘缺的早報。

## 每天的 5 分鐘

```bash
python brief.py --url "https://claude.ai/code/artifact/4ae34ac1-e44c-4c56-91fc-50e8005156d0"
```

會產出三個檔案到 `out/`:

| 檔案 | 用途 |
|---|---|
| `brief-YYYY-MM-DD.txt` | 貼到 LINE / Telegram 給付費者 |
| `brief-YYYY-MM-DD.html` | 網頁版 |
| `brief-YYYY-MM-DD.post.txt` | **今天的貼文草稿** |

然後:

1. 看一眼 `.post.txt`,**改一遍語氣**(30 秒)
2. 貼到 Threads,按送出
3. 有人留言就回

**不要跳過第 1 步。** 原封不動送出的自動貼文,平台會降觸及,而你會得到一個假的失敗結論。

其他參數:

```bash
python brief.py --date 2026-09-04            # 補跑某一天
python brief.py --portfolio 2330,2317,0050   # 換一組持股
```

---

## 已驗證的事

- **每天都有料**:回測 2026-08-19 ~ 09-04 共 13 個交易日,**13/13 天**都跑得出頭條訊號,平均 3.7 個
- **資料源可用**:FinMind 免費層、不用 token,台股 + 美股 + 匯率 + 營收 + 除息都拿得到
- **繞開授權風險**:全部使用日頻資料,**不碰台股即時報價**,避開了最大的法律問題

## 還沒驗證的事(這才是重點)

- 有沒有人願意付錢 ← Phase 1 要回答的唯一問題
- 哪一段(早報 / 法人 / 行事曆)才是他們要的
- 你發得出去嗎

---

## 訊號規則

`brief.py` 的 `signals()` 目前偵測八類,分數越高排越前面:

| 類型 | 條件 | 分數 | 回測觸發率 |
|---|---|---|---|
| `retail` | 外資賣超 + 融資餘額同步增加(散戶接刀) | 105+ | 38% 的日子 |
| `lend` | 借券加權費率 ≥ 自身 60 日 p90 且 ≥ 1% | 100+ | 9% 的(股,日) |
| `diverge` | 外資與投信反向,且雙方 \|z\| ≥ 0.8 | 100+ | — |
| `adr` | ADR 溢價單日變化 ≥ 1 個百分點 | 95+ | — |
| `us` | 美股觀察名單單日 \|漲跌\| ≥ 3% | 85+ | — |
| `z` | 外資買賣超 \|z\| ≥ 2(對比前 20 日) | 80+ | — |
| `streak` | 法人連續同向 ≥ 5 天(逆價走勢加權) | 60+ | — |
| `vol` | 量能 ≥ 2× 或 ≤ 0.5× 20 日均量 | 55 | — |

調整門檻直接改 `signals()` 裡的數字。ADR 對應表在 `ADR_MAP`(目前只有 2330 / 2303 / 2412 / 3711 有 ADR)。

### 借券費率為什麼用百分位而不是絕對值

各股的常態借券費率差很多——台積電中位數 0.42%、長榮 3.10%、廣達 5.42%。
用「5% 算高」這種絕對門檻會對廣達天天誤報、對台積電永遠不報。
所以門檻是**跟自己過去 60 天比的第 90 百分位**,自我校準,每檔股票的標準自動不同。

ETF(0050、00878)借券成交太少,湊不到 60 個樣本,這個訊號會自動跳過。

---

## 進度門檻

| 階段 | 門檻 | 沒過怎麼辦 |
|---|---|---|
| **Phase 1** | 14 天內 **5 個陌生人付費** | 問題在需求或訊息,不是產品。**不要開始寫 App** |
| Phase 2 | 20 個付費 + 續訂率 > 70% | 續訂率是真實價值的唯一證據 |
| Phase 3 | 才做 App(Expo / React Native) | App 的唯一意義是推播 |

**失敗診斷**(數字不好時,先確定斷在哪):

```
貼文曝光 < 500        → 渠道問題,換地方發
有曝光但點擊率 < 2%   → 訊息問題,改標題和第一句
有點擊但沒人填問卷    → 產品問題,第一屏看不懂
有人填但都選 NT$0     → 需求問題 ← 只有這個代表該放棄
```

前三種都跟寫程式無關。**大多數人失敗在前三種,卻回去改程式。**

---

## 看問卷結果

問卷答案存在樣本頁的資料庫裡(`responses` collection),匿名、不含任何個人資料。

要看結果就跟我說「讀一下問卷回覆」,我會撈出來統計。或你自己在對話裡問:

> 幫我看 https://claude.ai/code/artifact/4ae34ac1-e44c-4c56-91fc-50e8005156d0 的 responses 有幾筆、平均願付價格多少

---

## 已知限制

- **FinMind 免費層有流量限制。** `cache/` 會存 12 小時,重跑不會重複打 API。撞到上限時
  程式會**拒絕輸出**(exit 1)而不是給殘缺內容 —— 殘缺的早報會讓訂閱者把「抓失敗」
  誤讀成「這檔今天沒事」,那比沒有早報更糟
- **「該不該在意」目前是規則模板**,不是真的判斷。訊號偵測是自動的,但深度解讀還是要人。這是 Phase 2 的技術重點
- **美股新聞脈絡沒有自動化**(為什麼漲/跌)。目前要手動查。這是 A 段最貴、也最難被抄的部分
- 除息與營收公布日只涵蓋持股清單內的個股
- 所有輸出都是公開資料的統計描述,**不是投資建議**。對外務必保留免責聲明

---

## 檔案

| | |
|---|---|
| `brief.py` | 產生器(抓資料 → 算訊號 → 出早報 + 貼文草稿) |
| `brief-sample.html` | 已發布的樣本頁原始碼 |
| `out/` | 每日產出 |
| `cache/` | API 快取,可以隨時刪掉 |
